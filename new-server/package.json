{
    "name": "new-server",
    "module": "index.ts",
    "type": "module",
    "private": true,
    "scripts": {
        "start": "bun index.ts",
        "dev": "bun index.ts --debug",
        "build": "bun build index.ts --outdir dist --minify --target bun"
    },
    "devDependencies": {
        "@types/bun": "latest",
        "prettier": "^3.8.3"
    },
    "peerDependencies": {
        "typescript": "^5"
    },
    "dependencies": {
        "lowdb": "^7.0.1"
    }
}
